name: provider-boilerplate
runtime: yaml

resources:
  myRandomResource:
    type: provider-boilerplate:Random
    properties:
      length: 24
  myRandomComponent:
    type: provider-boilerplate:RandomComponent
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
